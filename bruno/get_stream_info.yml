info:
  name: Get Stream Info
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/stream/info"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
