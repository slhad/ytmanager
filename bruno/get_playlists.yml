info:
  name: Get Playlists
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/playlists?playlist=Streams"
  params:
    - name: playlist
      value: Streams
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
